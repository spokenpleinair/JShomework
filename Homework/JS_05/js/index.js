// Create the minute to seconds conversion function here.
function convertMinuteSeconds () {
    var minutes = 1;
    var seconds = 60;
    var totalSeconds = minutes * seconds;
    console.log (totalSeconds);
    }
    
    convertMinuteSeconds();
    
